# 联调 / 验收 / 发布进度

更新时间：2026-07-31。

## 已验证

- TypeScript 类型检查、Vitest 自动化测试和 Vite/Electron TypeScript 构建是当前基础门禁。
- Electron 与网页预览共用共享类型、IPC 契约和核心上下文逻辑。
- 已覆盖流式成功、停止、失败重试、会谈切换、删除/重命名、重启读回和跨会谈隔离。
- 已覆盖 rolling summary、token-first 预算、超长文本资料化、`txt/md` 附件和脱敏审计边界。
- 已覆盖会谈结束/继续、概念化、督导、备忘录、来信、旧周期失效与归档恢复状态。
- 已覆盖模型设置、知情说明、个人资料、阅读设置、Markdown 导出、备份恢复和密码保护的主要契约。
- 启动、等待室、咨询室、来信、归档与系统设置已做过多轮浏览器视觉检查。
- 本地语音模型已完成真实中英混合音频、流式 Worker、安装包内置资源校验/解压、macOS 打包与原生库装载验证；火山引擎 Seed-ASR、腾讯云实时语音识别 V2 和阿里云百炼实时 ASR 已完成独立配置入口、密钥隔离和流式协议接入，仍需分别使用真实服务商账号做端到端验收。
- 干净用户目录下的 Electron 首次启动、边界说明、跳过模型配置、等待室和系统设置已人工回归；系统凭据使用真实 Electron `safeStorage` 完成加密、读取与删除冒烟验证。
- 中文/英文界面、专业内容、提示词快照、后台材料和 Markdown 导出已接入统一语言设置；真实模型语言矩阵确认不需要逐消息自动路由，明确语言请求可稳定持续切换。
- 最新主线的语音输入与呼吸练习完成双语同步后，前后端类型检查、本地化完整性门禁、580 项 Vitest 和生产构建均通过。
- macOS arm64 应用包已完成实际启动冒烟，SQLite、sherpa、内置语音模型和双语麦克风权限文案均在包内校验；Windows x64 已在本地 Windows 11 ARM64 虚拟机的 x64 兼容环境中完成依赖安装、类型检查、580 项测试、生产构建和 NSIS 打包，SQLite、Windows sherpa DLL 与内置语音模型结构完整，未安装版和独立目录安装版均通过启动冒烟，正式 GUI 首屏可见。

## 发布前仍需验证

1. 继续干净用户目录的 Electron 全流程人工回归，补齐真实模型、会谈结束、归档、备份和密码恢复。
2. 使用无 Key、错误 Key、网络中断、模型限流和后台整理失败覆盖错误恢复。
3. 验证备份从旧 schema 恢复到新版本、损坏备份拒绝和恢复后的首次启动。
4. 验证 macOS/Windows 安装、升级、卸载和用户数据保留；native SQLite 与语音识别资源已纳入 Windows 打包启动工作流。
5. 使用真实 DeepSeek 模型做多组咨询与来信语言质量、安全边界人工审阅。
6. 使用真实火山引擎账号验证 Seed-ASR 2.0 鉴权、流式结束包、计费和限流错误提示。

## 当前发布阻塞

- 已有 macOS/Windows 打包配置和 Windows Release 工作流；仍缺 macOS Developer ID 签名/公证、Windows 正式代码签名及完整安装升级矩阵。
- 系统凭据存储仍需在正式 macOS/Windows 安装包中验证迁移与升级兼容。
- 真实模型质量和心理支持安全边界仍需持续人工验收。
- 英文内容正式发布前仍需双语心理专业、目标地区法律合规和危机资源审阅。
