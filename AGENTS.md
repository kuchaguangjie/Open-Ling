# Ling：Codex 开发规则

本文件是 Codex 在 Ling 项目中的长期工作约定。目标是让 Codex 直接、可靠地推进开发，避免为了流程而流程。

## 开始前

1. 阅读与本次任务直接相关的代码和文档；首次进入项目或缺少上下文时，再阅读 `README.md`、`docs/brief.md` 和 `docs/progress/README.md`。
2. 运行 `git status` 与 `git branch --show-current`，保留用户已有修改，不覆盖无关内容。
3. 明确本次任务的目标、影响范围和验收方式，然后直接执行；能从仓库确认的信息不要反复询问用户。

## 工作方式

- Codex 是本项目默认且唯一的编程工具，负责分析、设计、实现、测试、审查和文档更新。
- 小型、明确、低风险的任务直接实现，不要求先写任务卡、方案、handoff 或进度文档。
- 只有在需求较大、跨多个模块、需要分阶段推进、存在重要取舍，或用户明确要求时，才创建或更新任务卡、设计稿、决策记录与进度文档。
- 需要记录任务时，任务卡只写目标、范围和验收标准；完成情况直接写在任务卡或简洁的完成汇报中，不强制额外创建 handoff。
- 对重要 UI、视觉风格或关键交互，Codex 应先检查现有设计语言。方向不明确或改动成本高时先给出设计方案；局部且方向明确的改动可直接实现并进行视觉验证。
- 优先完成用户要的结果。不要为了满足文档流程而扩大修改范围，也不要重复记录已经能从 Git 历史和代码中确认的信息。

## 文档与进度

- 文档只在内容确实发生变化、能帮助后续开发时更新，避免每次代码修改都机械更新进度文件。
- `docs/progress/README.md` 记录项目级状态；`frontend.md`、`backend.md`、`integration.md` 仅在对应方向出现值得长期保留的进展、阻塞或下一步时更新。
- 给用户审阅的方案、任务卡、进度文档和产品说明默认使用中文；App 面向用户的界面文案默认使用中文。代码标识符、配置项和第三方 API 名称按工程惯例使用英文。
- 文档保持简洁、准确，过时内容应直接修正或删除。

## 代码与产品边界

- 按业务功能组织代码，不要把页面、数据库访问和业务规则堆进一个文件。
- 只有被两个以上模块真实复用的内容才提取为共享组件或工具。
- 页面不直接访问数据库；机密配置不写入代码。
- Ling 默认在本地保存设置、会谈、总结和记忆。
- 新功能根据实际风险处理加载、空状态、失败状态和权限边界。
- 注释解释“为什么”，不要重复代码本身。
- 不无故引入新的顶层目录、框架或架构层；确有必要时说明理由。

## Git 安全

- 功能开发使用 `feat/<short-name>` 分支；不要直接在 `main` 上开发。
- 开始修改前检查工作区，保留并避开用户已有的无关改动。
- 未经用户明确要求，不合并或推送 `main`，不删除分支，不改写历史。
- 禁止 `git reset --hard`、`git push --force`、`git clean -fd` 等破坏性操作。
- 不提交 `.env`、密钥、真实用户数据、录音、照片、本地数据库或导出文件。
- 提交前运行与改动相称的验证；无法验证的部分要明确说明。

## 完成汇报

默认简洁说明：

```text
完成内容：
修改文件：
验证：
风险/未完成项：（没有则省略）
```

只有用户需要协作交接、发布或审查时，才补充更详细的分支、提交、部署和下一步信息。
