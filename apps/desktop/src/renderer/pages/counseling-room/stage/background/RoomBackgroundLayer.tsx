/** C-005 owns this visual layer. Room image variables are supplied by the controller/page shell. */
export function RoomBackgroundLayer() {
  return <div className="portrait-backdrop" aria-hidden="true" />;
}
